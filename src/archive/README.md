# Archive

Superseded implementation kept for reference.

- **dashboard_v2.py** — Interactive world map + bar chart using Leaflet.js and Chart.js. Removed from the main pipeline because country coordinates required manual updates when countries changed.

## How to run

Requires gold JSON to exist (`data/gold/passengers_by_country.json`). Call `write_gold_dashboard_v2(gold_dir)` from `archive.dashboard_v2`.
